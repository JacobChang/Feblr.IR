akka {
  actor {
    provider = cluster
  }
  
  remote {
    dot-netty.tcp {
      hostname = localhost
      port = 4053
    }
  }            

  cluster {
    #will inject this node as a self-seed node at run-time
    seed-nodes = ["akka.tcp://crawler@localhost:4053"]
    roles=["seeds"]
  }
}