// Learn more about F# at http://fsharp.org
module Feblr.IR.Crawler

open System
open System.Net
open System.Threading.Tasks
open FSharp.Control.Tasks

open Feblr.IR.Crawler.Core

module Main =
    [<EntryPoint>]
    let main argv =
        0 // return an integer exit code
